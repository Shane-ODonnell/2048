void refresh() {
  image(Background, 0, 0);
}
/*
void mousePressed() {
  println(mouseX + ", " + mouseY);
}
for ( int i = 0; i < empty.length; i++) {
      empty[i] = true;
    }
    
    
      //removeTile(tiles[g].geti(), tiles[g].getj() - 1 );
//*/
